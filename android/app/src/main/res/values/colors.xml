<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Mirrors AppColorTheme.navy pageTop, so the native window, the Flutter
         splash and the app all share one ground with no flash between them. -->
    <color name="splash_navy">#050B1B</color>
</resources>
