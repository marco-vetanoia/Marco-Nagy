<?xml version="1.0" encoding="utf-8"?>
<!-- Native launch window: a flat navy ground shown by the OS before Flutter
     has started. It is replaced automatically once the engine draws its first
     frame, which is where the Flutter splash Scaffold takes over. -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:drawable="@color/splash_navy" />
</layer-list>
